# Revision brief — Book 18 essay 06 (apply the panel review)

Essay 06 of `/home/diablo/book18` (`chapters/06-grid-capacity-hubs.html`, `checks/claims/06.tsv`,
`resources/sources/06/`) has been reviewed. The consolidated review is `drafts/reviews/06-codex.md`;
the two underlying reports are `drafts/reviews/06-flash.md` and `06-pro.md` (the consolidator
already adjudicated them; where it rejected a reviewer suggestion, leave that alone).

Work only inside `/home/diablo/book18` and only in the essay-06 files plus `drafts/06.notes.md`
and `drafts/reviews/06-applied.md`. Do not touch other essays, `CONTEXT.md`, `index.html`,
`AGENT.md`. Finding 7 (navigation: 05 → 06 → 07 across three chapter files) is handled by the
coordinating session — skip it. All rules in `drafts/06.brief.md` and `drafts/06.rewrite.brief.md`
still bind (contract, priority stack, pre-ship test, HTML skeleton, claims schema, excerpt rule,
word bounds, voice).

Apply every other finding in the consolidated review, in rank order. In particular:
- Rebuild the financial table on net wholesale receipts: state exactly which lines carry the
  installer discount, break the delivery cost into headcount / specialist cover / cloud drivers,
  and recompute gross margin, operating surplus, working capital, peak cash need and the
  EUR 100,000-capital test from the corrected numbers. Every figure stays a design hurdle.
- Make the 90-day test's pass rule require a binding production order, deposit or paid first
  month at the stated hurdle, and make "paid baseline but no production order" an explicit
  kill or downgrade outcome.
- Narrow the ACM civil-liability sentence to what the saved excerpt supports (ACM declined to
  prescribe civil agreements between connected parties/groups and grid operators) and frame
  member/installer/software allocation as a private-contract design question, not an ACM holding.
- Repair the `06-contract-taxonomy` receipt: either a CBC-specific sentence with its own claim
  row and excerpt, or an expanded ledger row that keeps the narrower scope.
- Fix the verbless fragment in the economics paragraph; explain GOPACS / Congestion Service
  Provider and "peak shaving" on first use.

Then run `python3 checks/structure.py 06` until it passes, walk AGENT.md's pre-ship test, and
write `drafts/reviews/06-applied.md`: one numbered line per finding saying what changed.

Reply in at most 200 words: checker line, verdict (changed or not, and why), verified /
inference counts, excerpt count, anything left unresolved.
