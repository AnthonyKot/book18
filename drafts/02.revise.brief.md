# Revision brief — Book 18 essay 02 (apply the panel review)

Essay 02 of `/home/diablo/book18` (`chapters/02-white-label-product-security.html`, `checks/claims/02.tsv`,
`resources/sources/02/` if it exists) has been reviewed. The consolidated review is
`drafts/reviews/02-codex.md`; the two underlying reports are `drafts/reviews/02-flash.md` and
`02-pro.md` (the consolidator already adjudicated them; where it rejected a reviewer
suggestion, leave that alone).

Work only inside `/home/diablo/book18` and only in the essay-02 files plus
`drafts/reviews/02-applied.md`. Do not touch other essays, `CONTEXT.md`, `index.html`,
`AGENT.md`, `README.md`. All rules in `AGENT.md`, `TEMPLATE.md` and
`scripts/prompts/essay-brief.md` bind (contract in CONTEXT §6, priority stack, pre-ship test,
HTML skeleton, claims schema, word bounds, voice, sentences under 40 words). The saved-excerpt
rule applies from essay 6 onward; for this essay a `verified` row needs a URL that actually
carries the claim, re-fetched by you, or it is downgraded to `inference`/`open`.
Every figure in the economics stays a design hurdle, not a forecast. If a finding would change
the essay's verdict, apply the verdict the evidence supports and say so explicitly in the
applied log.

Apply every finding in the consolidated review, in rank order. In particular:
- Make the mature economics bottom-up: hours per baseline and per recurring account, loaded-role cost, utilisation, specialist and tool costs; add a monthly ramp cash bridge (receivables, payroll, VAT timing, max cash draw, capex) and test it against the EUR 100,000 ceiling.
- Move the 02-cra-roles marker to the sentence Articles 21–22 support and label the subcontract conclusion as an inference needing counsel, or add a separately receipted claim.
- Name and source two or three direct service alternatives (test/assurance firms, product-security consultancies) from their own material; compare scope, liability position and channel model.
- Replace 'proves' for the Cybellum–Hitachi announcement with what a first-party announcement establishes; apply the remaining low findings; footer to Seven candidates if stale.

Then run `python3 checks/structure.py 02` until it passes, walk AGENT.md's pre-ship test, and
write `drafts/reviews/02-applied.md`: one numbered line per finding saying what changed.

Reply in at most 200 words: checker line, verdict (changed or not, and why), verified /
inference counts, anything left unresolved.
