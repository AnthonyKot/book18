# Revision brief — Book 18 essay 07 (apply the panel review)

Essay 07 of `/home/diablo/book18` (`chapters/07-eudi-wallet-acceptance.html`, `checks/claims/07.tsv`,
`resources/sources/07/`) has been reviewed. The consolidated review is `drafts/reviews/07-codex.md`;
the two underlying reports are `drafts/reviews/07-flash.md` and `07-pro.md` (the consolidator
already adjudicated them; where it rejected a reviewer suggestion, leave that alone).

Work only inside `/home/diablo/book18` and only in the essay-07 files plus `drafts/07.notes.md`
and `drafts/reviews/07-applied.md`. Do not touch other essays, `CONTEXT.md`, `index.html`,
`AGENT.md`. All rules in `drafts/07.brief.md` still bind (contract, priority stack, pre-ship
test, HTML skeleton, claims schema, excerpt rule, word bounds, voice).

Apply every finding in the consolidated review, in rank order. In particular:
- Make the workload thresholds (delivery ceiling, price-support test, kill rule) one consistent
  model and recompute delivery cost and capacity from it.
- Resolve the Article 5b(10) intermediary-storage question in the architecture: say what the
  operator may hold and what must sit in a customer-controlled store, and rework the
  "accumulating asset" if the company cannot lawfully own the proposed history. Cite the
  article from a saved excerpt; do not invent counsel's view, say what would need counsel.
- Draw one explicit commercial and legal chain for customer twenty (who signs, invoices,
  registers as relying party, acts as intermediary, controls evidence, handles exceptions) and
  put partner margin into the mature economics with opening/added/churned/closing accounts.
- Add Regulation (EU) 2024/2982 to the source index, ledger and excerpts as the second trigger
  for the deadline, with the exact wording the review specifies.
- Complete every incomplete "verified" receipt the review lists: re-fetch, save the excerpt
  that actually carries the claim, or downgrade to `inference`.

Then run `python3 checks/structure.py 07` until it passes, walk AGENT.md's pre-ship test, and
write `drafts/reviews/07-applied.md`: one numbered line per finding saying what changed.

Reply in at most 200 words: checker line, verdict (changed or not, and why), verified /
inference counts, excerpt count, anything left unresolved.
