# Revision brief — Book 18 essay 01 (apply the panel review)

Essay 01 of `/home/diablo/book18` (`chapters/01-connected-product-operations.html`, `checks/claims/01.tsv`,
`resources/sources/01/` if it exists) has been reviewed. The consolidated review is
`drafts/reviews/01-codex.md`; the two underlying reports are `drafts/reviews/01-flash.md` and
`01-pro.md` (the consolidator already adjudicated them; where it rejected a reviewer
suggestion, leave that alone).

Work only inside `/home/diablo/book18` and only in the essay-01 files plus
`drafts/reviews/01-applied.md`. Do not touch other essays, `CONTEXT.md`, `index.html`,
`AGENT.md`, `README.md`. All rules in `AGENT.md`, `TEMPLATE.md` and
`scripts/prompts/essay-brief.md` bind (contract in CONTEXT §6, priority stack, pre-ship test,
HTML skeleton, claims schema, word bounds, voice, sentences under 40 words). The saved-excerpt
rule applies from essay 6 onward; for this essay a `verified` row needs a URL that actually
carries the claim, re-fetched by you, or it is downgraded to `inference`/`open`.
Every figure in the economics stays a design hurdle, not a forecast. If a finding would change
the essay's verdict, apply the verdict the evidence supports and say so explicitly in the
applied log.

Apply every finding in the consolidated review, in rank order. In particular:
- Resolve the TEST NOW vs required-bilingual-partner contradiction: either make the partner genuinely optional in the operating hypothesis or change the verdict to TEST THROUGH A PARTNER (and the verdict line in the header, ledger and closing).
- Name one accountable buyer with authority across engineering, quality, legal and product, or split the offer into a CRA operations baseline and a separately priced Data Act module with its own buyer.
- Move the 01-onekey and 01-data-dates markers to the sentences they actually support; fix the one ledger/source mismatch the review names.
- Make the year-one delivery-cost line transparent (labour, tooling, cloud drivers) without inventing the reviewer's numbers; split the three over-40-word sentences; footer to Seven candidates.

Then run `python3 checks/structure.py 01` until it passes, walk AGENT.md's pre-ship test, and
write `drafts/reviews/01-applied.md`: one numbered line per finding saying what changed.

Reply in at most 200 words: checker line, verdict (changed or not, and why), verified /
inference counts, anything left unresolved.
