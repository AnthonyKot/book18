# Revision brief — Book 18 essay 05 (apply the panel review)

Essay 05 of `/home/diablo/book18` (`chapters/05-product-passports.html`, `checks/claims/05.tsv`,
`resources/sources/05/` if it exists) has been reviewed. The consolidated review is
`drafts/reviews/05-codex.md`; the two underlying reports are `drafts/reviews/05-flash.md` and
`05-pro.md` (the consolidator already adjudicated them; where it rejected a reviewer
suggestion, leave that alone).

Work only inside `/home/diablo/book18` and only in the essay-05 files plus
`drafts/reviews/05-applied.md`. Do not touch other essays, `CONTEXT.md`, `index.html`,
`AGENT.md`, `README.md`. All rules in `AGENT.md`, `TEMPLATE.md` and
`scripts/prompts/essay-brief.md` bind (contract in CONTEXT §6, priority stack, pre-ship test,
HTML skeleton, claims schema, word bounds, voice, sentences under 40 words). The saved-excerpt
rule applies from essay 6 onward; for this essay a `verified` row needs a URL that actually
carries the claim, re-fetched by you, or it is downgraded to `inference`/`open`.
Every figure in the economics stays a design hurdle, not a forecast. If a finding would change
the essay's verdict, apply the verdict the evidence supports and say so explicitly in the
applied log.

Apply every finding in the consolidated review, in rank order. In particular:
- Replace the impossible six-month reserve with the correctly computed amount and show how advance billing, retained profit, receivables or a facility bridge the gap inside EUR 100,000.
- Compare the offer against Circularise field by field from its own product page; state what stays defensible (neutrality, second-life lineage, managed exceptions) and add a kill rule if buyers accept Circularise's native workflow.
- Add a workload bridge (automated-event share, exception minutes, onboarding hours, compliance-review hours) showing the stated team can deliver 12 customers / 4,000 events with contingency.
- Downgrade or re-receipt the Minespider open-API row; add immediate markers to the repeated external facts the review lists; apply remaining low findings; footer to Seven candidates if stale.

Then run `python3 checks/structure.py 05` until it passes, walk AGENT.md's pre-ship test, and
write `drafts/reviews/05-applied.md`: one numbered line per finding saying what changed.

Reply in at most 200 words: checker line, verdict (changed or not, and why), verified /
inference counts, anything left unresolved.
