# Revision brief — Book 18 essay 03 (apply the panel review)

Essay 03 of `/home/diablo/book18` (`chapters/03-machine-vision-food.html`, `checks/claims/03.tsv`,
`resources/sources/03/` if it exists) has been reviewed. The consolidated review is
`drafts/reviews/03-codex.md`; the two underlying reports are `drafts/reviews/03-flash.md` and
`03-pro.md` (the consolidator already adjudicated them; where it rejected a reviewer
suggestion, leave that alone).

Work only inside `/home/diablo/book18` and only in the essay-03 files plus
`drafts/reviews/03-applied.md`. Do not touch other essays, `CONTEXT.md`, `index.html`,
`AGENT.md`, `README.md`. All rules in `AGENT.md`, `TEMPLATE.md` and
`scripts/prompts/essay-brief.md` bind (contract in CONTEXT §6, priority stack, pre-ship test,
HTML skeleton, claims schema, word bounds, voice, sentences under 40 words). The saved-excerpt
rule applies from essay 6 onward; for this essay a `verified` row needs a URL that actually
carries the claim, re-fetched by you, or it is downgraded to `inference`/`open`.
Every figure in the economics stays a design hurdle, not a forecast. If a finding would change
the essay's verdict, apply the verdict the evidence supports and say so explicitly in the
applied log.

Apply every finding in the consolidated review, in rank order. In particular:
- Correct the global-shutter sentence (global shutter avoids line-by-line distortion; short exposure and illumination limit motion blur) and receipt it from Cognex's exposure documentation.
- Split or extend the 03-cognex-spec receipt so discrete I/O, Ethernet and IP67 are carried by the system-specifications page; receipt or remove the Keyence mention.
- Add the false-accept liability boundary to the economics: indemnity/cap assumptions, required liability cover, and a stress test of the mature result against one material claim.
- Specify the acceptance test's independent holdout set, minimum trials per defect class and condition, and separately reported false-accept/false-reject bounds; footer to Seven candidates.

Then run `python3 checks/structure.py 03` until it passes, walk AGENT.md's pre-ship test, and
write `drafts/reviews/03-applied.md`: one numbered line per finding saying what changed.

Reply in at most 200 words: checker line, verdict (changed or not, and why), verified /
inference counts, anything left unresolved.
