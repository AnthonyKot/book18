# Revision brief — Book 18 essay 04 (apply the panel review)

Essay 04 of `/home/diablo/book18` (`chapters/04-asset-intelligence.html`, `checks/claims/04.tsv`,
`resources/sources/04/` if it exists) has been reviewed. The consolidated review is
`drafts/reviews/04-codex.md`; the two underlying reports are `drafts/reviews/04-flash.md` and
`04-pro.md` (the consolidator already adjudicated them; where it rejected a reviewer
suggestion, leave that alone).

Work only inside `/home/diablo/book18` and only in the essay-04 files plus
`drafts/reviews/04-applied.md`. Do not touch other essays, `CONTEXT.md`, `index.html`,
`AGENT.md`, `README.md`. All rules in `AGENT.md`, `TEMPLATE.md` and
`scripts/prompts/essay-brief.md` bind (contract in CONTEXT §6, priority stack, pre-ship test,
HTML skeleton, claims schema, word bounds, voice, sentences under 40 words). The saved-excerpt
rule applies from essay 6 onward; for this essay a `verified` row needs a URL that actually
carries the claim, re-fetched by you, or it is downgraded to `inference`/`open`.
Every figure in the economics stays a design hurdle, not a forecast. If a finding would change
the essay's verdict, apply the verdict the evidence supports and say so explicitly in the
applied log.

Apply every finding in the consolidated review, in rank order. In particular:
- Define and use three units consistently (paying maintenance partner, end-customer plant, monitored site); state the partner/site ratio; describe the twentieth paying partner's acquisition route or justify the alternative gate; add partner-concentration and churn assumptions.
- Make the 90-day pass rule require a signed production order, paid onboarding deposit and first recurring invoice (or a binding end-customer-backed commitment); record the partner's resale price and margin.
- Fix the cash-buffer sentence (about EUR 54k for two months of payroll and contractors, or relabel as total cash operating cost); add a compact year 1 / year 3 / year 5 bridge.
- Repair the three receipt defects the review lists (Senseye ingestion wording and the other two); apply remaining low findings; footer to Seven candidates if stale.

Then run `python3 checks/structure.py 04` until it passes, walk AGENT.md's pre-ship test, and
write `drafts/reviews/04-applied.md`: one numbered line per finding saying what changed.

Reply in at most 200 words: checker line, verdict (changed or not, and why), verified /
inference counts, anything left unresolved.
